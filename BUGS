- Idle time is not accounted properly if selector is woken up spuriously or if
  someone calls selector.wakeup().

- Connection#send() thread-safety details are not documented.

- FIX message payload encoding is not handled at all which might corrupt data.
